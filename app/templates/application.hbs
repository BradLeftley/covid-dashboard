<div class="container">
    <div class="col-md-12">
        <h4 class="header">
            Daily Update
        </h4>
        {{log @model.data.firstObject.date}}
        <h5 class="subheader">{{moment-format @model.data.firstObject.date 'MMMM Do YYYY'}}</h5>
    </div>
    <DataCard @amount={{@model.data.firstObject.value}} />
</div>

{{outlet}}